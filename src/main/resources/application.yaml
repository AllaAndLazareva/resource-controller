spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/resources_controller
    username: root
    password: 4785375f
    driverClassName: com.mysql.cj.jdbc.Driver
    hikari:
      schema: controller
      graphql:
        tools:
          schema-location-pattern: '**/*.graphqls'
        playground:
          enabled: true
        servlet:
          max-query-depth: 5
          exception-handlers-enabled: true
