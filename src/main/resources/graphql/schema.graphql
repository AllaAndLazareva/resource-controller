type Query{
    realmId(id:ID):Realm}

type Realm{
    id:ID!
    description:String
    application:[Application]
}
type Application {
    id:ID!
    name: String
    description: String
    realm: Realm
}
